<div class="content-full">
  {{#if admin.isAuthenticated}}
    <div class="hover-tabs">
      <ul class="tab-list">
        <li>
          {{#link-to 'petpalsadmin.index' tagName="li"}}<a href="">Blog</a>{{/link-to}}
        </li>
        <li>
          {{#link-to 'petpalsadmin.teams' tagName="li"}}<a href="">Teams</a>{{/link-to}}
        </li>
        <li>
          {{#link-to 'petpalsadmin.schools' tagName="li"}}<a href="">Schools</a>{{/link-to}}
        </li>
      </ul>
    </div>
    <div class="content-body">
    {{outlet}}
    <div class="login-donate">
    <ul class="login-buttons">
      <li id="login-name">
    <p>Currently logged in as {{admin.currentUser.displayName}}</p>
  </li>
  <li>
    <button {{action "signOut" }} class="allbuttons admin-button">Sign out</button>
  </li>
</ul>
</div>
  </div>
  {{else}}
  <div class="signIn-button">
    <p>Please sign in to access admin pages</p>
    <button {{action "signIn" "google"}} class="allbuttons admin-button">Google Login</button>
  </div>
  {{/if}}
  </div>
