<h2>Teams Edit</h2>
<div id="teams-main">
  {{#each model as |teams|}}
    <div id="teams-container">
      <button  {{action 'deleteTeam' teams}} class="allbuttons"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete Team</button>
      <div class="team-img-cont">
      <div class="team-img">
            <img class="teams-Img" src="{{teams.image}}" />
        </div>
        <div id="teams-name">
            <h3 id="name">{{teams.name}}</h3>
        </div>
      </div>
      <div id="teams-bio">
        <p id='bio'>{{teams.bio}}</p>
      </div>
    </div>
  {{/each}}
</div>



<!-- <div class="post-footer">
  <button  {{action 'deleteTeam' teams}} class="allbuttons">Delete Team</button>
</div> -->

<!-- <div id="blog-main">
  {{#each model as |teams|}}
    <div id="blog-container">
      <div id="blog-title">
        <ul>
          <li>
            <img class="teamsImg" src="{{teams.image}}" />
          </li>
          <li>
            <h3 id="title">{{teams.name}}</h3>
          </li>
        </ul>
      </div>
      <div id="blog-body">
        <p>{{teams.bio}}</p>
      </div>
    </div>
    <button disabled= {{isDisabled}} {{action 'deleteTeam' model}} class="all-buttons">New Team</button>
  </li>
</ul>
{{#if responseMessage}}
  <div class="alert-success">{{responseMessage}}</div>
{{/if}}
  {{/each}}
</div> -->
