<h2>Schools Edit</h2>
<div class="list">
  {{#each model as |schools|}}
    <li><i class="fa fa-paw" aria-hidden="true"></i>{{schools.schools}}</li>
        <div class="post-footer">
          <button class="all-buttons" {{action 'deleteSchool' schools}}>Delete</button>
        </div>
  {{/each}}
</div>
